apiVersion: v1
kind: Service
metadata:
  name: nodeapp
  labels:
    type: dev
spec:
  selector:
    app: node
    type: prod
  ports: 
      - nodePort: 32000
        port: 80
        targetPort: 3000
  type: NodePort